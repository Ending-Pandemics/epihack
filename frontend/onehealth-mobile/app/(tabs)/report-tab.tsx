import React from 'react';
import { View } from 'react-native';

// Empty screen — center tab triggers modal via tab button onPress, never renders this
export default function ReportTab() {
  return <View style={{ flex: 1 }} />;
}
